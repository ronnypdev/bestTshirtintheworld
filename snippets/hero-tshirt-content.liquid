<section class="bg-[{{hero_background_color}}] w-full h-[60rem]">
  <div class="max-w-[1200px] text-center h-full mx-auto max-md:flex-col md:flex items-center justify-between">
    <div class="flex-auto">
      <h1 class="text-white font-bold font-mono">{{ hero_text }}</h1>
      <h3 class="text-white font-normal font-mono">{{ sub_header }}</h3>
    </div>
    <div class="flex-auto">
      <img src="{{ hero_image | image_url }}" alt="{{ hero_image.alt }}" width="326px" height="408px" class="mx-auto">
    </div>
  </div>
</section>